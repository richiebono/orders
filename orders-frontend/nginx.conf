server {
  listen 80;

  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri $uri/ /index.html;
    proxy_pass http://orders-backend.svc:8080;
  }

  include /etc/nginx/extra-conf.d/*.conf;
}
